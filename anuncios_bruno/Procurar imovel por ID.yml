info:
  name: Procurar imovel por ID
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/imoveis/2
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
