info:
  name: Listar Clientes
  type: http

http:
  method: GET
  url: http://localhost:8080/clientes/
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
