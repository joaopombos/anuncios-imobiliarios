info:
  name: Listar agentes
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/agentes/
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
